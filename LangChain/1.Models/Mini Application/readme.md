#This is a mini project application

Goal = To find the correct document from which the query belongs

1. Takes some documents as inputs first
2. Takes a query from the user as input
3. Checks through all the given documents for similarity
4. Returns the document with the highest similarity
